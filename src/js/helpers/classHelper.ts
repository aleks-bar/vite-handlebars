export const addClass = ( className: string, elem: HTMLElement ) => {
  elem.classList.add( className );
};
export const removeClass = ( className: string, elem: HTMLElement ) => {
  elem.classList.remove( className );
};
